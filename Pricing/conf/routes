POST /pricing/price         donebyme.pricing.controllers.PricingController.submit()
GET  /pricing/price/:id     donebyme.pricing.controllers.PricingController.price(id: String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
